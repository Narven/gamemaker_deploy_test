// Inherit the parent event
event_inherited();

// Set the bar's label
bar_name = "Music";

// Create a link between the switch's value and the global variable it should control
bar_percent = obj_sound_manager.bgm_level;